"1":"@ ensures size == \old(size) - (\sum int i; 1<=i && i<=\old(size));"
"2":"setElementData(i,obj);" "++newSize;"
"3": "return(MyObject) getElementData()[1];"
"4-1":"@ assignable heap, heap.elements, heap.size;"
"4-2":"@ requires count >= 0;@ requires  heap.size + count <= DEFAULT_CAPACITY;"
"4-3":"@ ensures heap.size == \old(heap.size) + count;"
"4-4": "@ ensures (\forall int i; 1 <= i && i <= \old(heap.size); heap.elements[i] == \old(heap.elements[i]));"
        "@ ensures (\forall int i; \old(heap.size) < i && i <= heap.size; heap.elements[i] instanceof MyObject);"
        "@ ensures (\forall int i; 1 <= i && i <= \old(heap.size); heap.contains(heap.elements[i]));"
         "@ ensures (\forall int i; \old(heap.size) < i && i <= heap.size; heap.contains(heap.elements[i]) || !heap.elements[i].isReferenced());"
"4-5":"@ requires count >= 0;@ requires heap.size + count > DEFAULT_CAPACITY;"
"4-6": "@ ensures heap.size == DEFAULT_CAPACITY;"
"4-7": "@ ensures (\forall int i; 1 <= i && i <= heap.size; heap.elements[i] instanceof MyObject);@ ensures (\forall int i; 1 <= i && i <= \old(heap.size); heap.contains(heap.elements[i]));"
"4-8": "@ ensures (\forall int i; \old(heap.size) < i && i <= heap.size; heap.contains(heap.elements[i]) || !heap.elements[i].isReferenced()); @ ensures (\forall int i; \old(heap.size) < i && i <= heap.size; !heap.contains(heap.elements[i]));"
